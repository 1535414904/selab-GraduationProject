import React from 'react';
import ReactDOM from 'react-dom/client';
import Gantt from '../src/Gantt';  // 引入 App 組件

const container = document.getElementById('root');
const root = ReactDOM.createRoot(container);

root.render(<Gantt />);

